# Contributing to XOBot

Спасибо за интерес к проекту XOBot! 🎉

## Как внести вклад

### 1. Fork и Clone

```bash
# Fork репозиторий на GitHub
# Затем склонируйте ваш fork
git clone https://github.com/your-username/XOBot.git
cd XOBot
```

### 2. Создайте ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

### 3. Настройте окружение

```bash
# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл из примера
cp .env.example .env
# Отредактируйте .env и добавьте ваш BOT_TOKEN
```

### 4. Внесите изменения

- Пишите чистый, читаемый код
- Следуйте стилю кода проекта (PEP 8 для Python)
- Добавляйте комментарии где необходимо
- Обновляйте документацию если нужно

### 5. Тестирование

```bash
# Запустите линтер
flake8 .

# Проверьте форматирование
black --check .

# Запустите тесты (если есть)
pytest
```

### 6. Коммит

```bash
# Добавьте изменения
git add .

# Сделайте коммит с понятным сообщением
git commit -m "feat: добавить новую функцию X"
# или
git commit -m "fix: исправить баг Y"
```

**Формат коммитов:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, конфигов и т.д.

### 7. Push и Pull Request

```bash
# Отправьте изменения в ваш fork
git push origin feature/your-feature-name

# Создайте Pull Request на GitHub
```

## Правила Pull Request

1. **Опишите изменения** - что было изменено и зачем
2. **Свяжите с issues** - если PR решает issue, укажите `Closes #123`
3. **Один PR = одна функция** - не смешивайте несколько изменений
4. **Обновляйте документацию** - если нужно
5. **Дождитесь ревью** - не мержите сами

## Стандарты кода

### Python

- Следуйте PEP 8
- Используйте type hints где возможно
- Пишите docstrings для функций и классов
- Максимальная длина строки: 120 символов

### JavaScript

- Используйте ES6+ синтаксис
- Комментируйте сложную логику
- Используйте `const` и `let`, избегайте `var`

## Структура проекта

```
XOBot/
├── bot.py              # Telegram Bot логика
├── api.py              # Flask API endpoints
├── database.py         # Работа с БД
├── config.py           # Конфигурация
├── promo_generator.py  # Генератор промокодов
├── run.py              # Entry point
├── webapp/             # Frontend (HTML/CSS/JS)
└── tests/              # Тесты (если есть)
```

## Вопросы?

Если у вас есть вопросы:
- Создайте Issue на GitHub
- Напишите в Discussions
- Свяжитесь с мейнтейнером

## Code of Conduct

Будьте уважительны к другим участникам. Мы все здесь чтобы учиться и помогать друг другу.

Спасибо за ваш вклад! 🙏
