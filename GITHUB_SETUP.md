# Инструкция по публикации на GitHub

Этот файл содержит инструкции по публикации проекта XOBot на GitHub.

## ✅ Что уже готово

Проект полностью подготовлен к публикации на GitHub:

- ✅ `.gitignore` - настроен для исключения секретов, временных файлов, зависимостей
- ✅ `LICENSE` - MIT лицензия
- ✅ `SECURITY.md` - политика безопасности
- ✅ `CONTRIBUTING.md` - руководство для контрибьюторов
- ✅ `CODE_OF_CONDUCT.md` - кодекс поведения
- ✅ `.env.example` - пример переменных окружения
- ✅ CI/CD workflows (`.github/workflows/`)
- ✅ Issue templates
- ✅ Pull Request template
- ✅ Dependabot конфигурация
- ✅ Pre-commit hooks конфигурация
- ✅ EditorConfig

## 🚀 Шаги для публикации

### 1. Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Заполните:
   - Repository name: `XOBot`
   - Description: `Telegram Tic Tac Toe Game Bot with promo codes`
   - Visibility: Public (или Private, если хотите)
   - **НЕ** добавляйте README, .gitignore или LICENSE (они уже есть)

### 2. Инициализируйте Git (если еще не сделано)

```bash
cd /home/user1/XOBot

# Проверьте статус
git status

# Если репозиторий не инициализирован
git init

# Добавьте все файлы
git add .

# Проверьте что github_token.txt НЕ добавлен
git status | grep github_token
# Должно быть пусто!

# Сделайте первый коммит
git commit -m "Initial commit: XOBot Telegram Tic Tac Toe Game Bot"
```

### 3. Подключите удаленный репозиторий

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/XOBot.git

# Или используйте SSH (если настроен)
git remote add origin git@github.com:YOUR_USERNAME/XOBot.git

# Проверьте
git remote -v
```

### 4. Отправьте код

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код
git push -u origin main
```

### 5. Настройте GitHub репозиторий

После публикации:

1. **Обновите CODEOWNERS**: Отредактируйте `.github/CODEOWNERS` и замените `@yourusername` на ваш GitHub username
2. **Настройте Secrets** (если нужны для CI/CD):
   - Settings → Secrets and variables → Actions
   - Добавьте необходимые секреты
3. **Включите GitHub Actions**: Settings → Actions → Allow all actions
4. **Настройте Dependabot**: Должен работать автоматически
5. **Добавьте темы (topics)**: 
   - telegram-bot
   - python
   - flask
   - tic-tac-toe
   - webapp
   - telegram-webapp

### 6. Проверьте безопасность

**ВАЖНО!** Перед публикацией убедитесь:

- [ ] `.env` файл НЕ в репозитории (проверьте `git status`)
- [ ] `github_token.txt` НЕ в репозитории
- [ ] Все секреты в `.env.example` заменены на примеры
- [ ] Нет хардкодных токенов в коде
- [ ] База данных (`*.db`) не коммитится

Проверка:
```bash
# Проверьте что секреты не попали в репозиторий
git log --all --full-history --source -- "*token*" "*secret*" "*password*"
git grep -i "token\|secret\|password" -- "*.py" "*.js" "*.md"
```

### 7. Обновите README.md

После публикации обновите ссылки в README.md:
- Замените `yourusername` на ваш GitHub username
- Обновите ссылки на WebApp (если нужно)

## 🔐 Использование GitHub Token

Если вам нужен GitHub token для автоматизации:

1. **Создайте Personal Access Token**:
   - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
   - Generate new token (classic)
   - Выберите нужные права (repo, workflow и т.д.)

2. **Сохраните токен безопасно**:
   - НЕ коммитьте в репозиторий!
   - Используйте GitHub Secrets для CI/CD
   - Или храните локально в `.env` (который в `.gitignore`)

3. **Используйте в CI/CD**:
   ```yaml
   env:
     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
   ```

## 📋 Чеклист перед публикацией

- [ ] Все секреты удалены из кода
- [ ] `.env` в `.gitignore`
- [ ] `github_token.txt` в `.gitignore`
- [ ] `.env.example` создан с примерами
- [ ] README.md обновлен
- [ ] LICENSE добавлен
- [ ] SECURITY.md создан
- [ ] CONTRIBUTING.md создан
- [ ] CI/CD workflows настроены
- [ ] Issue templates созданы
- [ ] CODEOWNERS обновлен с вашим username

## 🎉 Готово!

После выполнения всех шагов ваш проект будет готов к публикации на GitHub и соответствует всем стандартам GitHub Community!

## 📚 Полезные ссылки

- [GitHub Community Guidelines](https://docs.github.com/en/communities)
- [GitHub Security Best Practices](https://docs.github.com/en/code-security)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
